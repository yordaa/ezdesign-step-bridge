<!-- OPENSPEC:START -->
# OpenSpec Instructions

Always open `@/openspec/AGENTS.md` when a request:

- mentions planning, proposals, specifications, or changes;
- introduces a new capability, breaking change, architecture shift, or major
  performance/security work;
- is ambiguous enough to require the authoritative project specification.

Keep this managed block so `openspec update` can refresh the instructions.

<!-- OPENSPEC:END -->
